# Лента новостей по примеру ВК (React + Redux‑Saga, Vite)

[![Deploy frontend to GitHub Pages](https://github.com/stormozov/neto-saga-feed/actions/workflows/deploy.yml/badge.svg)](https://github.com/stormozov/neto-saga-feed/actions/workflows/deploy.yml)

Реализован упрощённый аналог подгрузки новостей «ВКонтакте». При открытии страницы отображаются первые записи, далее пользователь может до загружать предыдущие.

## API
Бэкенд находится в каталоге [backend](./backend).

API предоставляет доступ к пагинированным данным новостей. Клиент запрашивает порции новостей по 5 элементов, указывая идентификатор последней просмотренной новости.

⚠️ Сервер использует асинхронную функцию `fortune`, которая имитирует сетевой запрос с вероятностью успеха и случайной задержкой.

### Endpoints

### `GET /api/news`

Возвращает порцию новостей с пагинацией.

#### Параметры запроса (Query Parameters)

| Параметр     | Тип    | Обязательный | Описание |
|--------------|--------|--------------|----------|
| `lastSeenId` | string | Нет          | Идентификатор последней просмотренной новости. Если не указан — возвращается первая порция новостей. |

> ⚠️ Все параметры передаются в строке запроса (query string).

#### Успешный ответ

* **Код состояния**: `200 OK`  
* **Content-Type**: `application/json`  
* **Тело ответа**: Массив объектов новостей (максимум 5 элементов).

> 🔁 Если новостей больше нет после указанного lastSeenId, возвращается пустой массив [].

#### Возможные ошибки

| Код                        | Причина                    | Описание |
|----------------------------|----------------------------|----------|
| `400 Bad Request`          | Некорректный `lastSeenId`  | Параметр `lastSeenId` передан, но не может быть преобразован в число (например, строка `"abc"`). |
| `404 Not Found`            | Новость не найдена         | Указанный `lastSeenId` не соответствует ни одному имеющемуся ID в базе новостей. |
| `500 Internal Server Error`| Внутренняя ошибка          | Произошла непредвиденная ошибка при обработке запроса (например, сбой в модуле `fortune`). |

> 💡 Все ошибки возвращаются как текстовые сообщения в теле ответа (например, "Something went wrong"), а не в формате JSON.

#### Ограничения
* Максимальный размер одной порции (батча): 5 новостей.
* Все ID новостей — строки, содержащие целые числа (например, "1", "12").
* API не поддерживает сортировку, фильтрацию или поиск — только последовательную пагинацию.

## Замечания по развертыванию
* Для работы API требуется файл `./data/news.json` в корне проекта сервера.
* Сервер включает CORS — можно использовать из браузерного JavaScript без ограничений.

## Инструменты:

* [Vite](https://vitejs.dev/)
* [React](https://react.dev/)
* [TypeScript (TS)](https://www.typescriptlang.org/)
* [Redux Toolkit (RTK)](https://redux-toolkit.js.org/)
* [Redux Saga (RS)](https://redux-saga.js.org/)
* [Axios](https://axios-http.com/ru/)

## Реализация

1. Первичная загрузка

* При открытии страницы автоматически загружаются первые пять новостей.
* Новости отображаются в виде карточек.

2. Дозагрузка новостей

* Под списком новостей есть кнопка «К предыдущим записям».
* При нажатии на кнопку загружаются ещё пять новостей, которые добавляются в конец списка.
* Во время загрузки новостей отображается индикатор процесса.

3. Поведение при ошибках

* Приложение не уведомляет пользователя сообщениями или тостами.
* Автоматически повторяет запрос каждые 3 секунды, пока он не будет выполнен успешно (для текущей операции: первичной загрузки или дозагрузки).

4. Окончание новостей

* Если получено менее 5 элементов (включая 0), считать, что новости закончились.
* Не отображается кнопка «К предыдущим записям» и индикатор загрузки.

## Ограничения

* Пользователь не может создавать, редактировать или удалять записи.
* При ошибках — автоматический повтор, без уведомлений пользователя.
* Не использованы сторонние библиотеки для запросов данных (React Query, SWR и другие).